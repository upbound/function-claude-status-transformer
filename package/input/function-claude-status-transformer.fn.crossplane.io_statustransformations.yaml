---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: statustransformations.function-claude-status-transformer.fn.crossplane.io
spec:
  group: function-claude-status-transformer.fn.crossplane.io
  names:
    categories:
    - crossplane
    kind: StatusTransformation
    listKind: StatusTransformationList
    plural: statustransformations
    singular: statustransformation
  scope: Namespaced
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: StatusTransformation can be used to provide input to this Function.
        properties:
          additionalContext:
            description: |-
              AdditionalContext is additional context that the user may provide to help
              Claude identify the issue.
            type: string
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          aws:
            description: AWS defines authentication and Bedrock configurations.
            properties:
              bedrock:
                description: |-
                  AWSBedrock provides configurations for working with AWS Bedrock as a
                  model provider.
                properties:
                  modelID:
                    default: us.anthropic.claude-sonnet-4-20250514-v1:0
                    description: ModelID is the Claude model to be used.
                    type: string
                type: object
              region:
                description: Region specifies a specific region when this call is
                  applicable.
                type: string
            required:
            - bedrock
            type: object
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
        required:
        - additionalContext
        type: object
    served: true
    storage: true
